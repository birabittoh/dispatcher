# API Endpoints

- `GET /` - Serves the web interface
- `GET /api/sum?x=1&y=2` - Sums two numbers
- `GET /health` - Health check endpoint

## Examples

### Sum two integers via cURL

```bash
curl http://localhost:8080/api/sum?x=1&y=2
```
